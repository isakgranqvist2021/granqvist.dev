runtime: nodejs18
env: standard
instance_class: F1

handlers:
  - url: .*
    script: auto

automatic_scaling:
  min_idle_instances: automatic
  max_idle_instances: automatic
  min_pending_latency: automatic
  max_pending_latency: automatic
  max_concurrent_requests: 700
  max_instances: automatic

service_account: granqvist-dev@gothic-venture-373811.iam.gserviceaccount.com
